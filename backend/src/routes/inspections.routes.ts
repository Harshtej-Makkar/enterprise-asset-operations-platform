import { createStubRouter } from './stub-router.js';

/** Week 1 stub. Real implementation lands in Week 2 (Inspection list + dynamic form). */
export const inspectionsRouter = createStubRouter('inspections');
